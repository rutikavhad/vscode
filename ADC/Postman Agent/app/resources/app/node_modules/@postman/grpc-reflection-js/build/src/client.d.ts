import { ChannelCredentials, Metadata } from '@postman/grpc-js';
import { Root } from '@postman/protobufjs';
import type { ServerReflectionClient } from './reflection_providers/v1alpha/reflection_grpc_pb';
export declare class Client {
    metadata: Metadata;
    private fileDescriptorCache;
    private url;
    private credentials;
    private clientOptions;
    grpcClient: ServerReflectionClient | undefined;
    compatibleReflectionVersion: string | undefined;
    private reflectionResponseCache;
    private CompatibleServerReflectionRequest;
    constructor(url: string, credentials: ChannelCredentials, options?: object, metadata?: Metadata);
    private sendReflectionRequest;
    private evaluateSupportedServerReflectionProtocol;
    initialize(): Promise<void>;
    listServices(): Promise<string[]>;
    fileContainingSymbol(symbol: string): Promise<Root>;
    fileByFilename(filename: string): Promise<Root>;
    private resolveFileDescriptorSet;
    private resolveDescriptorRecursive;
    private getFileContainingSymbol;
    private getFilesByFilenames;
}
