"use strict";
/*
    In order to use the "oauth2" authentication type, you must implement:
        config.http.setHeader
*/
Object.defineProperty(exports, "__esModule", { value: true });
exports.oauth2 = oauth2;
const AUTHORIZATION_PREFIX = 'Bearer';
function oauth2(get) {
    const key = 'Authorization';
    let headerPrefix = get('headerPrefix');
    if (headerPrefix) {
        headerPrefix = headerPrefix.trim();
    }
    else {
        headerPrefix = AUTHORIZATION_PREFIX;
    }
    const value = `${headerPrefix} ${get('accessToken')}`;
    return [[key, value]];
}
//# sourceMappingURL=oauth2.js.map