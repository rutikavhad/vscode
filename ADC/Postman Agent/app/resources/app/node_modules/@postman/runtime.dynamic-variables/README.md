# dynamic-variables

Dynamic variables are pre-defined variables which evaluate to a different value every time they are resolved. This package provides a default set of dynamic variables and their respective generator functions, with the option to extend/override them.
