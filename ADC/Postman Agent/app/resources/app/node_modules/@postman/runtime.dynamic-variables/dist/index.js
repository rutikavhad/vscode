"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDynamicVariables = getDynamicVariables;
const default_generators_1 = __importDefault(require("./default-generators"));
function getDynamicVariables(overrides = {}) {
    const dynamicGenerators = {
        ...default_generators_1.default,
        ...overrides,
    };
    return objectKeys(dynamicGenerators).map((key) => {
        if (!key.match(/^\$.+/)) {
            throw new Error(`Invalid dynamic variable key: ${key}`);
        }
        return {
            key,
            value: dynamicGenerators[key].generator,
        };
    });
}
// Like Object.keys, but unsound in interest of more convenience
const objectKeys = (obj) => Object.keys(obj);
//# sourceMappingURL=index.js.map