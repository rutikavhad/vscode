import { Item } from '@postman/runtime.core';
declare const config: {
    supportedAuthTypes: ("noauth" | "basic" | "bearer" | "oauth2" | "apikey")[];
    http: {
        setHeader(item: Item, name: string, value: string | undefined): void;
    };
};
export default config;
