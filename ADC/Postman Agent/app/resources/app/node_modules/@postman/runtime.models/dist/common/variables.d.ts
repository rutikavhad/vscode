import { type InferModel } from '../model.js';
/**
 * A set of key-values to replace in requests.
 *
 * @example
 * ```ts
 * import { type Variables } from '@postman/runtime.models';
 *
 * const variables: Variables = [
 *   { key: 'host', value: 'http://localhost:9000' }
 * ];
 * ```
 */
export declare const Variables: import("../model.js").Model<{
    key: string;
    value?: unknown;
    disabled?: boolean | undefined;
}[], Record<never, unknown>>;
export type Variables = InferModel<typeof Variables>;
