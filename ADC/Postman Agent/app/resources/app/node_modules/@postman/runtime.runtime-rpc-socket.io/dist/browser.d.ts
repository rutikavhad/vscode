import * as rpc from '@postman/runtime.runtime-rpc';
export { MethodHandler, MethodContext, SessionData, } from '@postman/runtime.runtime-rpc';
import { type Socket } from 'socket.io';
export interface SocketIOOptions {
    socket: Socket;
    event?: string;
}
export declare class Client extends rpc.Client {
    constructor(options: SocketIOOptions);
}
