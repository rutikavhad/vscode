"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Client = void 0;
const rpc = __importStar(require("@postman/runtime.runtime-rpc"));
class Client extends rpc.Client {
    constructor(options) {
        super(async (controller) => {
            return setupConnection(options.socket, controller, options);
        }, { timeout: 0 });
    }
}
exports.Client = Client;
// Ties together a Socket.IO connection with an RPC session.
function setupConnection(socket, controller, options = {}) {
    const { event = '@postman/runtime' } = options;
    socket
        .on('disconnect', () => {
        controller.destroy();
    })
        .on('error', (error) => {
        controller.destroy(error);
    })
        .on(event, (data) => {
        if (data instanceof ArrayBuffer) {
            data = new Uint8Array(data);
        }
        if (!(data instanceof Uint8Array)) {
            console.error(new TypeError('Expected Uint8Array or ArrayBuffer message'));
            return;
        }
        controller.receive(data);
    });
    return {
        send(data) {
            socket.emit(event, data.buffer);
        },
        close(_reason) { },
        isOpen() {
            return socket.connected;
        },
    };
}
//# sourceMappingURL=browser.js.map