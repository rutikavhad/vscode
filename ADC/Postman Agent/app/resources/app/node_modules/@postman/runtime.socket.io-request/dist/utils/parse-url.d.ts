export type ParsedURL = {
    protocol: 'ws' | 'wss';
    url: string;
    port?: string;
    host: string;
    pathname: string;
    isTLS: boolean;
};
export declare function parseUrl(str: string): ParsedURL;
