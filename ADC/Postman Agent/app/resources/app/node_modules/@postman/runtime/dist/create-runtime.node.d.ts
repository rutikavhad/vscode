import { GRPCAgent } from '@postman/runtime.grpc-request';
import CompleteRuntime from './complete-runtime';
import { AnyEvent } from './events';
import { AnyItem } from './items';
import { type AnyItemWithOptions, type BuiltInItem, type InferItem, type Run as NextRun, type RuntimeConfig as NextRuntimeConfig, type RunContext } from './next.node';
export { ServerMethods } from './server-methods.node';
export interface RuntimeConfig<TAdditionalEvent extends AnyEvent = never, TAdditionalItem extends AnyItem | AnyItemWithOptions = never> extends NextRuntimeConfig<TAdditionalEvent, TAdditionalItem> {
    grpcRequest?: {
        resolveSchema?: GRPCAgent['resolveSchema'];
    };
}
export declare function createRuntime<TAdditionalEvent extends AnyEvent = never, TAdditionalItem extends AnyItem | AnyItemWithOptions = never>({ grpcRequest, ...nextConfig }?: RuntimeConfig<TAdditionalEvent, TAdditionalItem>): CompleteRuntime & {
    execute<TSpecificItem extends BuiltInItem | InferItem<TAdditionalItem>>(item: TSpecificItem, context?: RunContext<TAdditionalItem, TSpecificItem>): NextRun<TAdditionalEvent, TAdditionalItem, TSpecificItem>;
};
