import type { Item } from '@postman/runtime.models';
export type BuiltInItem = Item;
export interface AnyItem {
    type: string;
    id?: string;
    payload?: Record<string, unknown>;
    extensions?: Record<string, unknown>;
}
