import type { Channel, Platform } from '../../lib/rpc';
import type { Plugin } from '../../plugin';
import type { Run } from '../../run';
import { GRPCEvent, IncomingGRPCEvents, OutgoingGRPCEvents } from './grpc-event';
export interface GRPCPlatform extends Platform {
    invoke: (event: GRPCEvent.Invoke, run: Run['context']) => Promise<GRPCConnection>;
}
type GRPCConnection = Channel<IncomingGRPCEvents, OutgoingGRPCEvents>;
export declare function grpc(options: {
    platform?: GRPCPlatform;
}): Plugin;
export {};
