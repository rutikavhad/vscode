import type { HTTPRequest } from '@postman/runtime.models';
import { HashAlgorithm } from '../../lib/crypto/hashing';
import { FileSystemPlatform } from '../../lib/file-system/file-system';
import { HTTPEvent } from './http-event';
export declare function prepareBody(bodyInfo: HTTPRequest['payload']['body'], details?: {
    contentType?: string;
}, platform?: {
    fs?: FileSystemPlatform;
}): Promise<{
    body: Uint8Array;
    contentType: string | null;
    contentSize: number;
}>;
/**
 * Read then body for the given request
 */
export declare function readBody(request: Pick<HTTPEvent.SendRequest['payload'], 'body' | 'headers'>, platform?: {
    fs?: FileSystemPlatform;
}): Promise<Uint8Array>;
/**
 * Read the body for the given request and compute the hash
 * for the given hashing algorithm and encoding
 */
export declare function computeBodyHash(request: Pick<HTTPEvent.SendRequest['payload'], 'body' | 'headers'>, algorithm: HashAlgorithm, encoding: 'base64' | 'hex', platform?: {
    fs?: FileSystemPlatform;
}): Promise<string>;
