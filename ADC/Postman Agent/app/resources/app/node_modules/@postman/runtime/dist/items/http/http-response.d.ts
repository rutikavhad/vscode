import { KeyValues } from '@postman/runtime.models';
import { Event } from '../../events';
import { Run } from '../../run.js';
import { HTTPEvent } from './http-event.js';
export declare class Response {
    readonly headers: KeyValues;
    readonly ok: boolean;
    readonly redirected: boolean;
    readonly status: number;
    readonly statusText: string;
    readonly type: string;
    readonly url: string;
    downloadedBytes?: number;
    responseTime?: number;
    body: ReadableStream<Uint8Array>;
    constructor(run: Run, events: Event[], start: HTTPEvent.ReceivedResponseStart);
    get bodyUsed(): boolean;
    arrayBuffer(): Promise<ArrayBuffer>;
    bytes(): Promise<Uint8Array>;
    json<TValue extends unknown = unknown>(): Promise<TValue>;
    text(): Promise<string>;
    static from(run: Run): Promise<Response>;
}
