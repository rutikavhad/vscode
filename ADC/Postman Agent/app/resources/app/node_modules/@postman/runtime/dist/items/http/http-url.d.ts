import type { HTTPRequest } from '@postman/runtime.models';
import { type ParsedURL } from '../../lib/urls';
/**
 * Prepare URL from given HTTP request payload
 *
 * 1. Parse URL (with URL encoding based on setting)
 * 2. Set path variables
 * 3. Add explicit query parameters
 */
export declare function prepareURL(payload: HTTPRequest['payload']): ParsedURL;
