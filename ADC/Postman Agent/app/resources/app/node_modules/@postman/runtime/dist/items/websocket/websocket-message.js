"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MIME_TYPE_MAP = void 0;
exports.isJSON = isJSON;
exports.MIME_TYPE_MAP = {
    text: 'text/plain',
    html: 'text/html',
    json: 'application/json',
    xml: 'application/xml',
    binary: 'application/octet-stream',
};
/**
 * Simple JSON detection
 */
function isJSON(str) {
    const trimmed = str.trim();
    if (trimmed.length === 0)
        return false;
    const firstChar = trimmed[0];
    const lastChar = trimmed[trimmed.length - 1];
    // Check if it starts/ends like JSON
    if ((firstChar === '{' && lastChar === '}') ||
        (firstChar === '[' && lastChar === ']')) {
        try {
            JSON.parse(trimmed);
            return true;
        }
        catch {
            return false;
        }
    }
    return false;
}
//# sourceMappingURL=websocket-message.js.map