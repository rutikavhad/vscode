import { type TypedArray } from './typed-array';
/**
 * Chunk the given Array | TypedArray into chunks of the given size
 */
export declare function toChunked<TArray extends Array<unknown> | TypedArray>(array: TArray, size: number): TArray[];
/**
 * Mutating approach to filtering array
 */
export declare function filter<TValue>(array: TValue[], callback: (element: TValue, index: number, array: TValue[]) => boolean): TValue[];
