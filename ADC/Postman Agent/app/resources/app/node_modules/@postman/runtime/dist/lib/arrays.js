"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toChunked = toChunked;
exports.filter = filter;
const typed_array_1 = require("./typed-array");
/**
 * Chunk the given Array | TypedArray into chunks of the given size
 */
function toChunked(array, size) {
    const chunks = [];
    for (let i = 0; i < array.length; i += size) {
        const chunk = (0, typed_array_1.isTypedArray)(array) ?
            array.subarray(i, i + size)
            : array.slice(i, i + size);
        chunks.push(chunk);
    }
    return chunks;
}
/**
 * Mutating approach to filtering array
 */
function filter(array, callback) {
    let index = 0;
    while (index < array.length) {
        if (!callback(array[index], index, array)) {
            array.splice(index, 1);
        }
        else {
            // (only move index if not removing)
            index += 1;
        }
    }
    return array;
}
//# sourceMappingURL=arrays.js.map