/**
 * Create a set of cryptographically-random values of length
 */
export declare function randomValues(length: number): Uint8Array;
/**
 * Generate a cryptographically-random ASCII string of length
 */
export declare function randomASCII(length: number): string;
