"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.randomValues = randomValues;
exports.randomASCII = randomASCII;
/**
 * Create a set of cryptographically-random values of length
 */
function randomValues(length) {
    const values = new Uint8Array(length);
    crypto.getRandomValues(values);
    return values;
}
/**
 * Generate a cryptographically-random ASCII string of length
 */
function randomASCII(length) {
    const values = randomValues(Math.ceil(length / 2));
    let result = '';
    for (let i = 0; i < length; i += 1) {
        // ASCII printable range: 32-126
        const asciiCode = 32 + (values[i] % 95);
        result += String.fromCharCode(asciiCode);
    }
    return result;
}
//# sourceMappingURL=randomness.js.map