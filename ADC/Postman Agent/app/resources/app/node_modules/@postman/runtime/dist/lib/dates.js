"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTimestampInSeconds = getTimestampInSeconds;
exports.getNTTimestamp = getNTTimestamp;
function getTimestampInSeconds(date) {
    return Math.floor((typeof date === 'number' ? date : date.getTime()) / 1000);
}
/**
 * Generate "Windows NT time format" timestamp
 * (number of 100-nanosecond intervals since Jan 1, 1601 UTC)
 */
function getNTTimestamp(date) {
    // 11644473600000 = diff between 1970 and 1601
    const timestamp = (BigInt(typeof date === 'number' ? date : date.getTime()) +
        11644473600000n) *
        10000n;
    const dataView = new DataView(new ArrayBuffer(8));
    dataView.setBigUint64(0, timestamp, true);
    return new Uint8Array(dataView.buffer);
}
//# sourceMappingURL=dates.js.map