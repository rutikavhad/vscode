"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toError = toError;
function toError(unknownError) {
    if (unknownError instanceof Error) {
        return unknownError;
    }
    if (typeof unknownError !== 'object') {
        return new Error(String(unknownError));
    }
    if (unknownError == null || !('message' in unknownError)) {
        return new Error('Unknown error');
    }
    const error = new Error(String(unknownError.message));
    // Add extensions to error
    if ('name' in unknownError && typeof unknownError.name === 'string') {
        error.name = unknownError.name;
    }
    return error;
}
//# sourceMappingURL=errors.js.map