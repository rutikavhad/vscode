import { Channel, Platform } from '../rpc';
export interface FileSystem {
    readFile: (path: string) => Promise<Uint8Array>;
}
export interface FileSystemPlatform extends Platform {
    readFile: (path: string) => Promise<Channel<Uint8Array, never>>;
}
export declare function fs(platform: FileSystemPlatform): FileSystem;
