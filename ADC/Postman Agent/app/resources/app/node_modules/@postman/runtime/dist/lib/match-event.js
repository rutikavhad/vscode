"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.matchEvent = matchEvent;
function matchEvent(matcher, maybeEvent, ...args) {
    // Curry: create event handler for matcher
    if (!maybeEvent) {
        return typeof matcher === 'function' ? matcher : (function match(event, ...args) {
            return matchEvent.call(this, matcher, event, ...args);
        });
    }
    const event = maybeEvent;
    if (typeof matcher === 'function')
        return matcher.call(this, event, ...args);
    const handler = matcher[event.type];
    if (handler == null)
        return;
    return handler.call(this, event, ...args);
}
//# sourceMappingURL=match-event.js.map