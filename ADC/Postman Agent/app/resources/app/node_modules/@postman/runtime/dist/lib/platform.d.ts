import { BuiltInEvent, RunEvent } from '../events';
import { MCPEvent } from '../items/mcp';
import { Run } from '../run';
import { Channel } from './rpc';
export declare function forwardPlatformEventsToRun<ChannelEvent extends BuiltInEvent | RunEvent>({ run, event, callPlatform, errorType, }: {
    run: Run;
    event: {
        type: MCPEvent['type'];
    };
    errorType: string;
    callPlatform: () => Promise<Channel<ChannelEvent>>;
}): Promise<void>;
