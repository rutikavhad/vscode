export type MaybePromise<TValue> = Promise<TValue> | TValue;
export interface PromiseWithResolvers<TValue> {
    promise: Promise<TValue>;
    resolve: (value: TValue) => void;
    reject: (error: any) => void;
}
/**
 * Polyfill for Promise.withResolvers
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/withResolvers
 *
 * Baseline 2024 (available in Node.js 22+)
 */
export declare function promiseWithResolvers<TValue>(): PromiseWithResolvers<TValue>;
/**
 * Check if the given value is a Promise or Thenable
 */
export declare function isPromise<TValue = unknown>(value: unknown): value is Promise<TValue>;
/**
 * Convert callback-based method to Promise-based
 */
export declare function promisify<TArgs extends any[], TReturn>(fn: (...args: [...TArgs, (error: unknown, result?: TReturn) => void]) => void): (...args: TArgs) => Promise<TReturn>;
