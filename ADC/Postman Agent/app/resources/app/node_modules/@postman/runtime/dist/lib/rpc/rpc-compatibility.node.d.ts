import { ProxyResult } from '@postman/runtime.runtime-rpc';
import { EventEmitter } from 'node:events';
import { Channel } from '.';
export declare class ChannelEmitter<TIncoming, TOutgoing> extends EventEmitter<{
    data: [TIncoming];
    '#error': [unknown];
    '#close': [];
}> {
    #private;
    get closed(): boolean;
    constructor(channel: Channel<TIncoming, TOutgoing>);
    write(data: TOutgoing): Promise<void>;
    cancel(reason?: any): Promise<void>;
}
export declare function channelToProxy<TIncoming, TOutgoing>(channel: Channel<TIncoming, TOutgoing>): ProxyResult<ChannelEmitter<TIncoming, TOutgoing>>;
