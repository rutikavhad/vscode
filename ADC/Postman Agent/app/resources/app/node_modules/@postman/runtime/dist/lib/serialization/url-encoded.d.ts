import { HTTPRequest } from '@postman/runtime.models';
export type URLEncoded = Extract<HTTPRequest['payload']['body'], {
    type: 'urlencoded';
}>['content'];
export declare function serializeURLEncoded(data: URLEncoded): string;
