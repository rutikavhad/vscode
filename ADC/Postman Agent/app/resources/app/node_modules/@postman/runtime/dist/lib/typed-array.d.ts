export type TypedArray = Uint8Array | Uint8ClampedArray | Uint16Array | Uint32Array | Int8Array | Int16Array | Int32Array | Float32Array | Float64Array | BigInt64Array | BigUint64Array;
export declare function isTypedArray(value: unknown): value is TypedArray;
/**
 * Ensure given value is a TypedArray, converted ArrayBuffer and DataView to Uint8Array
 */
export declare function toTypedArray<TBuffer extends ArrayBufferLike | TypedArray | DataView>(buffer: TBuffer): TBuffer extends TypedArray ? TBuffer : Uint8Array;
/**
 * Combine a set of buffers (ArrayBuffer | TypedArray | DataView) into the given TypedArray
 *
 * @example
 * ```ts
 * const values = [
 *   new Uint8Array([97]),
 *   new Uint8Array([98]),
 *   new Uint8Array([99]),
 * ];
 * const result = combineInto(Uint8Array, values);
 * const text = new TextDecoder().decode(result); // 'abc'
 * ```
 */
export declare function combineInto<TArray extends TypedArray>(Constructor: {
    new (length: number): TArray;
    new (buffer: ArrayBuffer, byteOffset?: number, length?: number): TArray;
}, arrays: Array<ArrayBuffer | TArray | DataView>): TArray;
/**
 * Limit the length of the given data, truncating if necessary
 */
export declare function limitLength<TArray extends TypedArray>(data: TArray, length: number): TArray;
