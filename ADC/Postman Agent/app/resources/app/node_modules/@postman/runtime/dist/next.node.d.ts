import type { AnyEvent } from './events.js';
import type { AnyItem } from './items.js';
import type { AnyItemWithOptions } from './plugin.js';
import { Runtime as BaseRuntime, type RuntimeConfig } from './runtime.js';
export { isAnyEvent, isErrorEvent, isEventOfType, isRuntimeEvent, isRuntimeEventContext, isRuntimeEventOfType, type AnyEvent, type BuiltInEvent, type ErrorEvent, type Event, type InternalEvent, type RunEvent, type RuntimeEvent, type RuntimeEventContext, } from './events.js';
export type { AnyItem, BuiltInItem } from './items.js';
export type { AnyItemWithOptions, InferItem, InferItemOptions, ItemOptions, Plugin, } from './plugin.js';
export { Run, type RunContext } from './run.js';
export { phases, type Phase, type RuntimeConfig, type RuntimePlatform, } from './runtime.js';
/**
 * Create new instance of the Postman Runtime
 *
 * @example
 * ```ts
 * const runtime = new Runtime();
 * const run = runtime.execute({
 *   type: 'collection',
 *   children: [...]
 * });
 *
 * for await (const event of run) {
 *   console.log(event);
 * }
 * ```
 */
export declare class Runtime<TAdditionalEvent extends AnyEvent = never, TAdditionalItem extends AnyItem | AnyItemWithOptions = never> extends BaseRuntime<TAdditionalEvent, TAdditionalItem> {
    constructor(config?: RuntimeConfig<TAdditionalEvent, TAdditionalItem>);
}
