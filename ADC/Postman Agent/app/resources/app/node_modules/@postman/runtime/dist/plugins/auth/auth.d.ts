import { Auth } from '@postman/runtime.models';
import { Run } from '../../run';
export declare function resolveAuth<const Type>(run: Run, type: Type): Extract<Auth, {
    type: Type;
}> | undefined;
