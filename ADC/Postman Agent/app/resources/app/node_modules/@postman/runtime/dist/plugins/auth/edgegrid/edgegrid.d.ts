/**
 * Generate an Authorization header for EdgeGrid
 */
export declare function generateEdgeGridAuthorization(parameters: {
    accessToken: string;
    clientToken: string;
    clientSecret: string;
    nonce: string;
    timestamp: string;
    bodyHash?: string;
    headers: Record<string, string>;
    headersToSign: string[];
    method: string;
    baseURL?: string;
    url: {
        protocol: string;
        host: string;
        path: string;
    };
}): Promise<string>;
/**
 * Normalize headers and combine them into tab-separated string
 */
export declare function canonicalizeHeaders(headersToSign: string[], headers: Record<string, string>): string;
/**
 * Use special date formatting for timestamps in EdgeGrid
 */
export declare function formatTimestamp(date: Date): string;
