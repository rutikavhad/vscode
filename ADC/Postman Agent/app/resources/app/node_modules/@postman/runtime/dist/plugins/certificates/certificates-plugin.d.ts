import { Plugin } from '../../plugin';
export interface FileDefinition {
    src: string;
}
export interface CertificateDefinition {
    /**
     * Note: postman-collection uses "matches" for Certificate and "match" for Proxy
     * chose to _unify_ on "match" for @postman/runtime.
     * Technically, Proxy match is `string` and Certificate matches is `string[]`, but unify on `match` accepting one-or-more patterns
     */
    match: string | string[] | {
        pattern: string | string[];
    };
    /**
     * Certificate in PEM format (e.g. .crt or .pem)
     */
    cert?: FileDefinition;
    /**
     * Private key file in PEM format (e.g. .key or .pem)
     */
    key?: FileDefinition;
    /**
     * PFX file in PEM format (e.g. .pfk or .pem) that includes public and private key
     */
    pfx?: FileDefinition;
    passphrase?: string;
}
export interface CertificateContext {
    definitions?: CertificateDefinition[];
    /**
     * CA file in PEM format
     */
    ca?: FileDefinition;
}
export declare const certificates: Plugin;
