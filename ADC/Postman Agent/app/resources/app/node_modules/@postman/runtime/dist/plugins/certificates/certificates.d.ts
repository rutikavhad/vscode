import { RuntimePlatform } from '../../runtime';
import type { CertificateContext } from './certificates-plugin';
export interface Certificate {
    cert?: Uint8Array;
    key?: Uint8Array;
    pfx?: Uint8Array;
    passphrase?: string;
}
export declare function loadCertificate(platform: RuntimePlatform, context: CertificateContext, url: string): Promise<{
    certificate: Certificate | null;
    ca: Uint8Array | null;
}>;
