import { Cookie, CookieStore } from 'postman-request';
import { AnyEvent } from '../../events';
import { ChannelController } from '../../lib/rpc';
/**
 * CookieStore that handles incoming/outoing cookie events on behalf of the given channel controller
 *
 * @example
 * ```ts
 * const channel = new Channel(controller => {
 *   const jar = request.jar(new CookieConnectionStore(controller));
 * })
 * ```
 */
export declare class CookieConnectionStore implements CookieStore {
    #private;
    constructor(controller: ChannelController<AnyEvent, AnyEvent>);
    findCookie(domain: string, path: string | null, key: string, callback: (error: {} | null, value: Cookie | null) => void): void;
    findCookies(domain: string, path: string | null, allowSpecialUseDomain: boolean | undefined, callback: (error: {} | null, value: Cookie[]) => void): void;
    putCookie(cookie: Cookie, callback: (error: {} | null, value: undefined) => void): void;
}
