import type { CookieJar as BuiltInCookieJar, CookieStore } from 'postman-request';
export type { Cookie } from 'postman-request';
export interface CookieJar {
    store: CookieStore;
    getCookieString(url: string): Promise<string>;
    setCookie(cookie: string, url: string): Promise<void>;
}
export declare function normalizeCookieJar(jar: BuiltInCookieJar): CookieJar;
