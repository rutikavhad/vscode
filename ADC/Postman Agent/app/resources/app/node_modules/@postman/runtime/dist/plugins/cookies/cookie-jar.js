"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeCookieJar = normalizeCookieJar;
function normalizeCookieJar(jar) {
    return {
        store: jar.store,
        async getCookieString(url) {
            return new Promise((resolve, reject) => {
                jar.getCookieString(url, (error, cookieString) => {
                    if (error) {
                        reject(error);
                    }
                    else {
                        resolve(cookieString);
                    }
                });
            });
        },
        async setCookie(cookie, url) {
            return new Promise((resolve, reject) => {
                jar.setCookie(cookie, url, (error) => {
                    if (error) {
                        reject(error);
                    }
                    else {
                        resolve();
                    }
                });
            });
        },
    };
}
//# sourceMappingURL=cookie-jar.js.map