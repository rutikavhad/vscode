import type { Cookie, CookieStore } from 'postman-request';
export type { CookieStore };
/**
 * Normalize the given store to use a Promise-based approach
 */
export declare function normalizeCookieStore(store: CookieStore): {
    findCookies: (domain: string, path: string | null, allowSpecialUseDomain: boolean | undefined) => Promise<Cookie[]>;
    putCookie: (cookie: Cookie) => Promise<void>;
};
