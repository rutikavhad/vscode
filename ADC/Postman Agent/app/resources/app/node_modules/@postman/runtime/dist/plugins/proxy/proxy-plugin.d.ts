import { Channel, Platform } from '../../lib/rpc';
import { Plugin } from '../../plugin';
interface ProxyConfigDefinition {
    match?: string | {
        pattern: string;
    };
    host?: string;
    port?: number;
    protocol?: string;
    tunnel?: boolean;
    disabled?: boolean;
    authenticate?: boolean;
    username?: string;
    password?: string;
}
export interface ProxyContext {
    definitions?: ProxyConfigDefinition[];
    /**
     * Use proxy information from system running @postman/runtime
     * (e.g. use Chromium's proxy resolution in Electron)
     */
    useSystem?: boolean;
    /**
     * Use proxy information from environment variables
     * (e.g. http_proxy, NO_PROXY, etc.)
     */
    useEnvironment?: boolean;
}
export interface ProxyPlatform extends Platform {
    resolveProxy: (url: string, options?: {
        useSystem?: boolean;
        useEnvironment?: boolean;
    }) => Promise<Channel<string | null, never>>;
}
export declare const proxy: Plugin;
export {};
