import { type Events as MaybeEventHandlers } from '@postman/runtime.models';
import { Run } from '../../../run';
import type { SandboxFleetExecutor } from '../sandbox';
type EventHandlers = Array<{
    owner?: string;
    listen: string;
    script: NonNullable<MaybeEventHandlers[number]['script']>;
}>;
export declare function attachDefaultEventListeners(run: Run, execution: Awaited<ReturnType<SandboxFleetExecutor['execute']>>, signal: AbortSignal): void;
export declare function collectEventHandlers(run: Run, listen: {
    item: string;
    ancestor: string;
}): EventHandlers;
export declare function defaultExecuteOptions(run: Run): Pick<Parameters<SandboxFleetExecutor['execute']>[0], 'variableProvider' | 'sendRequestProvider' | 'vault' | 'packageProvider'>;
export {};
