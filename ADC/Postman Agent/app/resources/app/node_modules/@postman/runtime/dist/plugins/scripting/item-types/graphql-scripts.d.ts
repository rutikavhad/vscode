import { GraphQLEvent } from '../../../items/graphql';
import { Plugin } from '../../../plugin';
import { Run } from '../../../run';
export declare class GraphQLScripts implements Plugin {
    #private;
    name: string;
    script: {
        'graphql:send-request': (event: GraphQLEvent.SendRequest, run: Run) => Promise<void>;
        'graphql:received-response': (event: GraphQLEvent.ReceivedResponse, run: Run) => Promise<void>;
    };
}
