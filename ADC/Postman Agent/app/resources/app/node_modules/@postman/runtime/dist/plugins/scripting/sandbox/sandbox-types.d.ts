import { Event } from '@postman/runtime.core';
import type { ExecutionContext } from 'postman-sandbox';
export type ItemContext = Omit<ExecutionContext, '_variables' | 'collectionVariables' | 'globals' | 'environment'>;
export interface SentEvent {
    data: unknown;
    source: {
        id: string;
        name?: string;
    };
}
export type HTTPResponseEvent = Event<'extension:events:http-response', {
    id: string;
    eventId: string;
    err: unknown;
    response: unknown;
}>;
