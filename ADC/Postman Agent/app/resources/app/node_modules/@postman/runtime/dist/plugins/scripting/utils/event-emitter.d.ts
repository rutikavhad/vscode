export declare class EventEmitter<T extends {
    [K in keyof T]: any;
}> {
    private handlers;
    addEventListener<K extends keyof T & string>(event: K, handler: (e: T[K]) => void, opts?: {
        signal?: AbortSignal;
        once?: boolean;
    }): void;
    removeEventListener<K extends keyof T>(event: K, handler: (e: T[K]) => void): void;
    offAll(): void;
    emit<K extends keyof T>(event: K, arg: T[K]): void;
}
