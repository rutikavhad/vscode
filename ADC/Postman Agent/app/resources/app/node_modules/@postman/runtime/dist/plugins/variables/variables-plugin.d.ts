import { MaybePromise } from '../../lib/promises';
import { Plugin } from '../../plugin';
import { VariableValues } from './variables';
export interface Vault {
    values: (scope?: {
        url?: string;
    }) => MaybePromise<VariableValues>;
    setValue?: (key: string, value: unknown, scope?: {
        url?: string;
    }) => MaybePromise<void>;
}
export declare const variables: Plugin;
