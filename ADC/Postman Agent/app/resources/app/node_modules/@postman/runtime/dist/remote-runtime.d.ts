import { GRPCAgent } from '@postman/runtime.grpc-request';
import { Client } from '@postman/runtime.runtime-rpc';
import CompleteRuntime from './complete-runtime';
import { AnyEvent } from './events';
import { AnyItem } from './items';
import { type BuiltInItem, type Run as NextRun, type RuntimeConfig as NextRuntimeConfig, type RunContext } from './next';
import type { AnyItemWithOptions, InferItem } from './plugin';
export interface RemoteRuntimeConfig<TAdditionalEvent extends AnyEvent = never, TAdditionalItem extends AnyItem | AnyItemWithOptions = never> extends NextRuntimeConfig<TAdditionalEvent, TAdditionalItem> {
    client: Client;
    grpcRequest?: {
        resolveSchema?: GRPCAgent['resolveSchema'];
    };
}
export declare function createRemoteRuntime<TAdditionalEvent extends AnyEvent = never, TAdditionalItem extends AnyItem | AnyItemWithOptions = never>({ client, grpcRequest, ...nextConfig }: RemoteRuntimeConfig<TAdditionalEvent, TAdditionalItem>): CompleteRuntime & {
    execute<TSpecificItem extends BuiltInItem | InferItem<TAdditionalItem>>(item: TSpecificItem, context?: RunContext<TAdditionalItem, TSpecificItem>): NextRun<TAdditionalEvent, TAdditionalItem, TSpecificItem>;
};
