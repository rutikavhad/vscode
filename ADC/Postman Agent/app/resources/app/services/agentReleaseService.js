var AgentReleaseService = {
  /**
   * Used to return the release channel
   *
   * @returns {String}
   */
  getReleaseChannel () {
    return 'stable';
  }
};

module.exports = AgentReleaseService;
