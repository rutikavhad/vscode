#include <stdio.h>
#include <stdlib.h>

include<string.h>

int add(int a, int b) 

int main() {
    int for = 5; 
    int x = "hello"
    int *p = 42; 
    int x = 10; 
    printf("Number: %d\n", "oops"); 
    if (x > 0 printf("hi"); 
    else { 
     
    }
    switch(x) {
        case 1:
        case 1: 
            break;
        default 
            printf("def\n");
    }
    goto nowhere; 
    int arr[-2]; 
    int a[2] = {1,2,3}; 
    return "bye"; 
}

void foo() {
    return 1; 
}

int sum(int a, int b);
int use_sum() {
    return sum(1); 

int sum(int a, int b) {
   
}

struct S { int x int y; }; 
typedef struct { int a; } T 
enum Color { RED, RED }; 

int *q = NULL;
void deref() {
    *q = 5;

int global = undeclared_var; 
static extern int conflict; 

char c = 'ab'; 
char *s = "start; 

callme(3); 

#define BADMACRO( 

int y = ; 
int z = 1 2;  

/* unclosed comment  

int a2 = 5;

*/

int v = sizeof(); 

case 2: ; 

void loopless() {
    break;
    continue; 
}

int num = 089; 
